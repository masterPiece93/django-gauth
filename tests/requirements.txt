Django
# .env parsing
django-environ
# self
-e .

environs
# automation
selenium
undetected-chromedriver
setuptools
# coverage
# `[toml]` extra is required so coverage can read its config from pyproject.toml
# on Python < 3.11 (which lacks the stdlib `tomllib`). No-op on 3.11+.
coverage[toml]